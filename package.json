{
  "devDependencies": {
    "eslint": "^8.2.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.25.2"
  },
  "scripts": {
    "lint": "eslint . --fix",
    "lint:watch": "nodemon --exec 'eslint . --fix' --watch ."
  }
}
